#include <cs50.h>
#include <stdio.h>
#include <stdbool.h>


int main(int a, int b, int c)
{
    bool isTrue = true;
    bool isFalse = false;
}
if (isTrue) {
        // This block will execute because isTrue is true.
       for int triangle (a, b, c)
    }